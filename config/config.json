{
  "development": {
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "dialect": "mysql",
    "logging": true
  },
  "test": {
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME_TEST}",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "dialect": "mysql",
    "logging": false
  },
  "production": {
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME_PROD}",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "dialect": "mysql",
    "logging": false
  }
}
